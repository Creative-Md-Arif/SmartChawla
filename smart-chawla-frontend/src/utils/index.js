import axiosInstance from './axiosInstance';
import {
  formatPrice,
  formatDate,
  formatRelativeTime,
  formatNumber,
  slugify,
  truncateText,
  validateEmail,
  validatePhoneBD,
  generateOrderId,
  calculateDiscountPercentage,
  formatFileSize,
  capitalize,
  formatDuration,
} from './formatters';

export {
  axiosInstance,
  formatPrice,
  formatDate,
  formatRelativeTime,
  formatNumber,
  slugify,
  truncateText,
  validateEmail,
  validatePhoneBD,
  generateOrderId,
  calculateDiscountPercentage,
  formatFileSize,
  capitalize,
  formatDuration,
};
